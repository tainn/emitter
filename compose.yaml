services:
  emitter:
    build:
      context: .
    image: emitter:0.1.2
    container_name: emitter
    restart: unless-stopped
    networks:
      - int
    env_file:
      - .env
    labels:
      - "tainn.type=app"
    entrypoint: [ "uv", "run", "/app/src/main.py" ]

networks:
  int:
    external: true
