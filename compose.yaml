services:
  emitter:
    build:
      context: .
    image: emitter:0.1.4
    container_name: emitter
    restart: unless-stopped
    networks:
      - int
    env_file:
      - .env
    entrypoint: [ "uv", "run", "/app/src/main.py" ]

networks:
  int:
    external: true
